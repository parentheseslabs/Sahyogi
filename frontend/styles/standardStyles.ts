// Standardized styles to be used across all components

export const standardHeadingStyle: React.CSSProperties = {
  fontSize: 'clamp(2.5rem, 6vw, 3.5rem)',
  fontWeight: 900,
  color: '#1a355e',
  letterSpacing: '-0.02em',
  marginBottom: 'clamp(1rem, 2vw, 1.5rem)',
  lineHeight: 1.2,
  textAlign: 'left',
};

export const standardSubheadingStyle: React.CSSProperties = {
  fontSize: 'clamp(1.8rem, 4vw, 2.4rem)',
  fontWeight: 700,
  color: '#1a355e',
  letterSpacing: '-0.01em',
  marginBottom: 'clamp(0.8rem, 2vw, 1rem)',
  lineHeight: 1.3,
  textAlign: 'left',
};

export const standardBodyTextStyle: React.CSSProperties = {
  fontSize: 'clamp(1.1rem, 3vw, 1.3rem)',
  color: '#4a5568',
  fontWeight: 500,
  lineHeight: 1.6,
  marginBottom: 'clamp(0.8rem, 2vw, 1rem)',
  textAlign: 'left',
};

export const standardSectionTitleStyle: React.CSSProperties = {
  fontSize: 'clamp(2rem, 5vw, 2.8rem)',
  fontWeight: 800,
  color: '#1a355e',
  letterSpacing: '-0.015em',
  marginBottom: 'clamp(1rem, 2vw, 1.5rem)',
  lineHeight: 1.25,
  textAlign: 'center',
};
